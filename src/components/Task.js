import React from 'react';

const Task = () => {

    return (
        <div className='task'>
            <p>Заголовок</p>
            <button>Удалить</button>
        </div>
    );
};

export default Task;
